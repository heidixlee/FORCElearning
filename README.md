# FORCElearning
An implementation of FORCE learning through which a recurrent neural network with random, noisy, and sparse connections was taught to combine neural activity to create a target sine function.
